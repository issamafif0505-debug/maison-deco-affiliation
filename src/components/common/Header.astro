---
import navigation from '../../data/navigation.json';
import siteConfig from '../../data/site-config.json';

const currentPath = Astro.url.pathname;
---

<!-- Barre d'annonce -->
<div class="announcement-bar hidden sm:block">
  <a href="/produits/" class="hover:underline transition-colors">
    🎁 +64 produits testés et approuvés — <span class="underline">Voir les meilleures offres du moment</span> →
  </a>
</div>

<header class="bg-white/95 backdrop-blur-md border-b border-gray-100 sticky top-0 z-50 shadow-sm">
  <nav class="container-custom">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-2.5 group flex-shrink-0">
        <div class="w-9 h-9 bg-gradient-to-br from-primary-500 to-accent-500 rounded-xl flex items-center justify-center shadow-sm group-hover:shadow-md transition-all">
          <span class="text-white text-base">🏠</span>
        </div>
        <div class="hidden sm:block">
          <span class="font-serif font-bold text-base text-gray-900 group-hover:text-primary-600 transition-colors leading-tight block">
            {siteConfig.siteName}
          </span>
          <span class="text-xs text-gray-400 leading-none">Décoration & Bons plans</span>
        </div>
      </a>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center gap-1">
        {navigation.map((item) => (
          <a
            href={item.href}
            class:list={[
              'px-3 py-2 rounded-lg text-sm font-medium transition-all duration-200',
              currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href))
                ? 'bg-primary-50 text-primary-700'
                : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900',
            ]}
          >
            {item.label}
          </a>
        ))}
      </div>

      <!-- Right side -->
      <div class="flex items-center gap-2">
        <a
          href="/produits/"
          class="hidden md:flex items-center gap-1.5 px-3 py-2 text-xs text-gray-500 bg-gray-50 hover:bg-gray-100 rounded-lg transition-colors border border-gray-200"
          aria-label="Voir tous les produits"
        >
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0" />
          </svg>
          <span>Produits...</span>
        </a>
        <a href="/produits/" class="hidden md:inline-flex btn-primary text-sm py-2 px-4">
          🔥 Best-sellers
        </a>

        <!-- Mobile Menu Button -->
        <button
          id="mobile-menu-btn"
          class="md:hidden p-2 rounded-xl hover:bg-gray-100 transition-colors"
          aria-label="Menu"
          aria-expanded="false"
        >
          <svg class="w-5 h-5 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path id="icon-open" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            <path id="icon-close" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile Navigation -->
    <div id="mobile-menu" class="hidden md:hidden py-3 border-t border-gray-100">
      <div class="flex flex-col gap-1">
        {navigation.map((item) => (
          <a
            href={item.href}
            class:list={[
              'px-4 py-3 rounded-xl text-sm font-medium transition-colors',
              currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href))
                ? 'bg-primary-50 text-primary-700'
                : 'text-gray-600 hover:bg-gray-50',
            ]}
          >
            {item.label}
          </a>
        ))}
        <div class="pt-2 border-t border-gray-100 mt-1">
          <a href="/produits/" class="btn-primary text-sm py-2.5 px-4 w-full text-center block">
            🔥 Voir les best-sellers
          </a>
        </div>
      </div>
    </div>
  </nav>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const iconOpen = document.getElementById('icon-open');
  const iconClose = document.getElementById('icon-close');

  btn?.addEventListener('click', () => {
    const isOpen = !menu?.classList.contains('hidden');
    menu?.classList.toggle('hidden');
    if (iconOpen && iconClose) {
      iconOpen.classList.toggle('hidden', !isOpen);
      iconClose.classList.toggle('hidden', isOpen);
    }
    btn.setAttribute('aria-expanded', String(!isOpen));
  });
</script>
